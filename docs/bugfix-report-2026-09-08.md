# ผลตรวจและแก้บั๊ก 8 กันยายน 2026

## ขอบเขตและข้อจำกัด

ตรวจโค้ดและรัน TypeScript, lint, production build พร้อม regression tests สำหรับบั๊กที่ทำซ้ำได้ ใช้ PGlite ทดสอบธุรกรรมและสิทธิ์ก่อนลง migration ในฐานข้อมูล Supabase ของ doc-tracking ตามที่ผู้ใช้อนุญาต การเปลี่ยนสิทธิ์ไม่ได้ลบข้อมูลธุรกิจ

การทดสอบนี้ไม่ได้ยืนยันว่าไม่มีบั๊กเหลือทั้งหมด และไม่ใช่การทดสอบทุก workflow ผ่านบัญชีผู้ใช้จริง

## จุดที่แก้

1. **ตรวจยืนยันปิดงานไม่ครบธุรกรรม** — เปลี่ยนการเขียนสถานะกับหลักฐานเป็น `verify_document_delivery` RPC เดียว เมื่อการเขียนส่วนใดล้มเหลว Postgres ยกเลิกทั้งธุรกรรม ไม่พึ่งคำขอ rollback ทางเครือข่ายอีกครั้ง
2. **รับเอกสารแล้วหลักฐานหายเมื่อ insert ล้มเหลว** — เพิ่ม `receive_document` RPC ให้การเปลี่ยนสถานะและบันทึกลายเซ็นสำเร็จพร้อมกัน ใช้ row lock และตรวจสถานะใต้ล็อก ป้องกันรับซ้ำ ทั้งสอง RPC เรียกได้เฉพาะ service_role
3. **ลบเอกสารข้ามหน่วยงานโดยไม่ตั้งใจ** — ไม่ตีความ count ที่ล้มเหลวเป็นศูนย์ จึงไม่ลบเอกสารแม่และข้อมูลหน่วยงานอื่นผ่าน cascade เมื่ออ่านจำนวนปลายทางไม่ได้
4. **เห็นรายการแต่เปิดรายละเอียดไม่ได้** — ใช้หลักสิทธิ์อ่านเดียวกับหน้ารายการ ได้แก่ผู้ดูแล ผู้บันทึก ผู้ที่เคยเซ็น และผู้มีสิทธิ์ตามแผนก/ขั้นตอน โดยยังปฏิเสธผู้ไม่เกี่ยวข้อง
5. **ผลค้นหา 6 รายการถูกผลเก่า 1 รายการเขียนทับ** — รับผลคำขอล่าสุดเท่านั้นในแท็บปิดงานแล้ว และล้างผลเก่าพร้อมแจ้งข้อผิดพลาดเมื่อค้นหาไม่สำเร็จ ทดสอบ component จริงด้วยข้อมูล 6 เอกสารจากผู้ส่งเดียวกัน
6. **การแจ้งเตือนว่างทั้งที่ Redis มีข้อมูล** — ไม่ parse JSON ซ้ำหลัง SDK แปลงให้อัตโนมัติ ทดสอบกับ Redis SDK จริงโดยจำลองเฉพาะการตอบกลับจากเครือข่าย
7. **migration 023 รันซ้ำล้มเหลว** — ลบ policy เดิมด้วย IF EXISTS ก่อนสร้างใหม่ และทดสอบรัน SQL สองครั้ง
8. **TypeScript อนุมานชนิด Supabase ผิดจน build ต้องข้ามการตรวจ** — ระบุชนิด client อย่างชัดเจน แก้ union ผลลงทะเบียนและชนิดรายการแผนก แล้วนำ `ignoreBuildErrors` ออก ทั้งนี้ยังไม่ได้สร้าง generated database types ให้ทุกตาราง
9. **แพ็กเกจที่มีประกาศช่องโหว่** — อัปเดต dependency, กำหนด PostCSS 8.5.28 ให้ Next.js และอัปเกรด `@vercel/blob` เป็น 2.8.0 / `googleapis` เป็น 150.0.1 โดยคง Next.js 15 / React 18 ปรับ `addRandomSuffix: true` เพื่อรักษาพฤติกรรมชื่อไฟล์ไม่ซ้ำหลังอัปเกรด Blob
10. **ปิดงานสำเร็จแต่ตอบล้มเหลวเมื่อ Sheets ขัดข้อง** — แยกข้อผิดพลาดการซิงก์หลัง commit ออกจากผลบันทึกหลัก ป้องกันผู้ใช้กดส่งซ้ำเพราะเข้าใจว่ายังไม่สำเร็จ
11. **สิทธิ์ฐานข้อมูลกว้างเกินจำเป็น** — ปิดการเขียนโดยตรงผ่าน anon/authenticated, จำกัด RPC ให้ service_role, เปิด RLS ของตาราง archive, ให้ summary view เคารพสิทธิ์ผู้เรียก และกำหนด search_path ของฟังก์ชัน
12. **service-role key อยู่ในสคริปต์ที่ติดตามด้วย Git** — เปลี่ยนให้รับจาก environment, หมุนเวียน API key, ปิด legacy keys และเพิกถอนลายเซ็น HS256 เดิมแล้ว ยืนยันคีย์ที่เคยอยู่ใน Git ใช้ไม่ได้ทั้งแบบ apikey และ Bearer token (HTTP 401) ไม่แก้ประวัติ Git โดยอัตโนมัติ

## หลักฐานทดสอบ

- `npm test`: regression/integration tests 27 ผ่าน และชุดทดสอบเงิน 26 ผ่าน
- บั๊กหลักมีการทดสอบให้ล้มเหลวก่อนแก้ และผ่านหลังแก้ (TDD)
- `npm run lint`: ไม่มี error แต่ยังมี 22 warnings เดิม เช่น unused variables, hook dependencies และ `<img>`
- `npm run build`: ผ่าน รวมขั้นตรวจชนิดข้อมูลและสร้าง 41 static pages
- `npm run typecheck`: ผ่านเมื่อรันแยกหลัง build จบ (ไม่รันพร้อม build ซึ่งสร้าง/ล้าง `.next/types`)
- `git diff --check`: ผ่าน
- `npm audit`: ไม่พบช่องโหว่ที่เครื่องมือรู้จัก ณ เวลาตรวจ ทั้ง production และ development dependencies; ไม่ใช่การรับรองว่าไม่มีช่องโหว่ที่ยังไม่ถูกค้นพบ
- ทดสอบการอัปโหลดผ่าน Blob SDK จริงโดยใช้ network interceptor ที่ปิดการเชื่อมต่อภายนอก และทดสอบ Google SDK สร้างคำขอ Sheets/Drive รวมถึงการสร้าง OAuth/service-account clients โดยไม่ได้ส่งไฟล์หรือติดต่อบัญชีจริง
- การ build แจ้งว่า environment นี้ไม่มีค่า Redis และมีคำเตือนเกี่ยวกับ Supabase ใน Edge Runtime; ไม่ได้ยืนยันว่าการแจ้งเตือนออนไลน์ทำงานบน environment นี้

## ฐานข้อมูลจริงและการนำขึ้นใช้งาน

ลง migration `20260908013605_atomic_delivery_verification.sql` และ `20260908013649_database_security_lockdown.sql` สำเร็จแล้ว ปรับชื่อ migration แรกให้ตรงกับเวลาที่ Supabase บันทึกจริง

`node scripts/verify-database-security.mjs` ตรวจผ่านฐานข้อมูลที่เชื่อมกับ Supabase CLI: ตาราง/วิว 24 รายการ ฟังก์ชัน 25 รายการ ไม่มีสิทธิ์เขียนผ่าน browser และ server ยังเข้าถึงได้ สคริปต์นี้ใช้ Management API โดยไม่ปิดการตรวจใบรับรอง TLS

ประวัติ migration เก่า 017–024 ยังไม่ถูกบันทึกใน remote แม้มีโครงสร้างที่เกี่ยวข้องอยู่แล้ว จึงไม่รันซ้ำหรือรับรองประวัติย้อนหลังโดยเดา

Supabase security advisor เหลือคำเตือน Leaked Password Protection; ทดลองเปิดผ่าน Management API แล้วได้รับ HTTP 402 PaymentRequired จึงไม่เปลี่ยนแพ็กเกจหรือสร้างค่าใช้จ่ายเอง

สร้าง API key คู่ใหม่ อัปเดต 6 ตัวแปรที่เกี่ยวข้องครบ Production/Preview/Development และอัปเดตเฉพาะตัวแปร Supabase ที่มีอยู่ใน `.env.local` โดยรักษาการตั้งค่าอื่น ตรวจผ่าน Auth และ REST ด้วย SDK จริง

Push โค้ด commit `09ad6cf` ไปยัง master สำเร็จ และ Production deployment `dpl_2cKjWZQsQWWxnF4GQA5xU82UR7cC` สถานะ READY ที่ https://doc-tracking-system-three.vercel.app

หลัง deployment ปิด legacy API keys และเพิกถอน legacy HS256 signing key โดยไม่เปลี่ยน ES256 signing key ปัจจุบัน ตรวจเว็บจริงพบ public key ใหม่ใน client bundle และไม่พบ server secret ในไฟล์ที่โหลดมาตรวจ หน้าแรกตอบ 200; API documents/profile/setup ปฏิเสธผู้ไม่ล็อกอินด้วย 401 การทดสอบนี้ไม่ได้แทนการตรวจทุก workflow ด้วยบัญชีจริง

ผู้ใช้ที่ยังเปิดหน้าเว็บรุ่นเก่าอาจต้องรีเฟรช และ session ที่ลงนามด้วย HS256 เดิมอาจต้องเข้าสู่ระบบใหม่

## บทเรียนการเพิกถอนคีย์

- **Trigger:** ต้องยกเลิก Supabase legacy service-role JWT ที่เคยเปิดเผย
- **Action:** อัปเดตแอปเป็นคีย์ใหม่ก่อน ปิด legacy API keys และตรวจคำขอทั้ง apikey เดิมกับ publishable key ใหม่ร่วมกับ Bearer เดิม; หากยังผ่าน ต้องตรวจสถานะ legacy signing key และเพิกถอนความเชื่อถือเดิมด้วย
- **Evidence:** การทดสอบครั้งนี้พบ 401 สำหรับ apikey แต่ยังได้ 200 สำหรับ Bearer จนเพิกถอน HS256 และรอการตั้งค่ามีผล จากนั้นทั้งคู่ตอบ 401 โดย SDK คีย์ใหม่ยังผ่าน
- **Scope:** local; **Status:** validated; **Reviewed:** 2026-09-08

ให้ทดสอบบน staging เพิ่ม: รับเอกสารปกติ/ใบรับสินค้า, ปฏิเสธและส่งมอบใหม่, ยืนยันปิดงาน, ลงนามหลายรายการ, สิทธิ์แต่ละแผนก, การอัปโหลดไฟล์ และการซิงก์ Google Sheets โดย Blob SDK ใหม่ต้องใช้ Node.js 20 ขึ้นไป; การตรวจครั้งนี้ใช้ Node.js 24.15.0

## ประเด็นที่ยังไม่สรุปแทนผู้ใช้

- อาการ 6 เหลือ 1 ทำซ้ำได้จากผลค้นหาย้อนลำดับ แต่ยังไม่ยืนยันว่าเป็นเหตุการณ์เดียวกับที่ผู้ใช้พบจริง ต้องทราบหน้าจอ ตัวกรอง และเอกสารที่เกี่ยวข้อง
- ยังไม่ได้เปลี่ยนกฎเอกสารซ้ำ เพราะต้องกำหนดก่อนว่าเลขเอกสาร ผู้ส่ง วันที่ และหน่วยงานใดเป็นตัวตัดสินความซ้ำ
- ตรวจ offline/draft แบบอ่านอย่างเดียว: หน้าลงทะเบียนเก็บแบบฟอร์มใน React state; หน้ารับ/ฝากเงินบันทึกบางช่องก่อนส่ง แต่ไม่เก็บรูป; service worker ไม่มีคิวส่งข้อมูลออฟไลน์หรือซิงก์อัตโนมัติ

แนวทางที่ใช้: systematic-debugging, test-driven-development, verification-before-completion, Supabase และ Vercel Storage โดยแนวทาง Storage ช่วยตรวจการเปลี่ยนค่าเริ่มต้นชื่อไฟล์ก่อนอัปเกรด

อ้างอิงการเปลี่ยนรุ่น: [Vercel Blob changelog](https://github.com/vercel/storage/blob/main/packages/blob/CHANGELOG.md) ระบุการเปลี่ยนค่าเริ่มต้น `addRandomSuffix`; [Google API client changelog](https://github.com/googleapis/google-api-nodejs-client/blob/main/CHANGELOG.md) ระบุการอัปเกรด dependency และ Node.js ขั้นต่ำของรุ่น 150
